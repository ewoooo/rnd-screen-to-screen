/**
 * payment-kit 토큰 — home-kit/pilot-kit SSOT 그대로 따른다.
 * (Figma 04_ADP_P3-T1_Mockup 의 #FFEAEA / #D54949 / #1A1A1A / #666 같은 raw hex 는 사용하지 않는다.
 * 색은 home-kit T_BRAND + var(--semantic-*), 카드는 CARD_BG/CARD_BORDER/CARD_RADIUS,
 * 간격은 var(--spacing-*) 로 일치시킨다.)
 */
export {
	BADGE_BG,
	CARD_BG,
	CARD_BORDER,
	CARD_RADIUS,
	GNB_BORDER,
	PAGE_BG_SEMI,
	T_BRAND,
	T_BRAND_SHADOW,
} from "@/components/home-kit/tokens";
