/**
 * home organism 토큰은 앱 전역 브랜드 토큰의 alias.
 * SSOT는 `@/lib/brand-tokens`. 신규 사용처는 lib 쪽을 직접 import 할 것.
 */
export {
	BADGE_BG,
	CARD_BG,
	CARD_BORDER,
	CARD_RADIUS,
	CARD_SHADOW,
	FONT,
	GNB_BORDER,
	OFFERING_BG,
	OFFERING_BORDER,
	type FontStyle,
} from "@/lib/brand-tokens";
